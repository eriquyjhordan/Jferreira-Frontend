import { HomeContainer, HomeTitle } from '../styles/home.styled';

export default function Home() {
  return (
    <HomeContainer>
      <HomeTitle>Start a new project</HomeTitle>
    </HomeContainer>
  );
}
